{% extends "layouts/main.njk" %}
{% block body %}

      <h3 class="text-white text-center bg-dark">Area Clienti</h3>
      <hr style="border-top: 1px solid #5375BD;">

      <form role="form" action="/login" method="post" autocomplete="on">
        <div class="form-group">
          <!-- <label  for="inputUsernameEmail" class="text-white col-form-label-lg">Utente</label> -->
          <div class="input-error" id="wrongMail"></div>
          <input type="email" class="form-control" id="inputUsernameEmail" name="email" value="{{ email }}" placeholder="Indirizzo email" autofocus required autocomplete="username" >
        </div>
        <div class="form-group">

          <div class="contenitore">
            <input type="password" class="form-control" id="inputPassword" name="password" placeholder="Password" autocomplete="current-password">
            <a class="password-toggle-icon"><i class="bi bi-eye" id="togglePassword"></i></a>
          </div>

        </div>

        <div>
          <a class="text-right nav-link" href="/forgot">Password dimenticata?</a>
        </div>
        <div style="margin-bottom: 40px;">
          <button type="submit" class="btn btn-my btn-lg btn-block" id="mybutton" onclick="return fieldLoginControl()">
            Accedi
          </button>
        </div>
        <input type="hidden" name="returnTo" value={{returnTo}} />
      </form>

      <!--div class="login-or">
        <hr class="hr-or">
        <span class="span-or">or</span>
      </div>

      <div class="row">
        <div class="col-xs-6 col-sm-6 col-md-6">
          <a href="#" class="btn btn-lg btn-primary btn-block">Facebook</a>
        </div>
        <div class="col-xs-6 col-sm-6 col-md-6">
          <a href="#" class="btn btn-lg btn-info btn-block">Google</a>
        </div>
      </div-->
      <script type="text/javascript">
        // Svuota sempre la password al caricamento della pagina
        document.addEventListener('DOMContentLoaded', function(){
          var pwd = document.getElementById('inputPassword');
          if (pwd) pwd.value = '';
        });

        const passwordField = document.getElementById("inputPassword");
        const togglePassword = document.querySelector(".password-toggle-icon i");

        togglePassword.addEventListener("click", function () {
          if (passwordField.type === "password") {
            passwordField.type = "text";
            togglePassword.classList.remove("bi-eye");
            togglePassword.classList.add("bi-eye-slash");
          } else {
            passwordField.type = "password";
            togglePassword.classList.remove("bi-eye-slash");
            togglePassword.classList.add("bi-eye");
          }
        });
      </script>

{% endblock %}
